# QR Code Encryption Keys for SCM Multichain
# Generate 32-byte (64 hex characters) random keys for production

# AES-256 encryption key (32 bytes = 64 hex characters)
QR_AES_KEY=replace_with_64_character_hex_key

# HMAC verification key (32 bytes = 64 hex characters)  
QR_HMAC_KEY=replace_with_64_character_hex_key

# Example of how to generate keys:
# python3 -c "import secrets; print('QR_AES_KEY=' + secrets.token_hex(32))"
# python3 -c "import secrets; print('QR_HMAC_KEY=' + secrets.token_hex(32))"